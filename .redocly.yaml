apis:
  # a set of named keys and values of entrypoints to OpenAPI definitions.
  stash-api: ./openapi.yaml

lint:
  extends:
    - recommended

features.openapi:
  # functionality and theming options
